import requestsimport jsonserver_url = "http://localhost:5000"username = ""logged_in = Falsedef login(new_username):    global username    global logged_in    if (logged_in):        print "Already logged in"    else:        r = requests.post(server_url + "/accounts/login/", data = {'username': username})        if (r.status_code == 200):            username = new_username            logged_in = True        else:             print "Network failure."def create_account(new_username):    if (logged_in):        print "Can't create account when logged in"    else:         r = requests.post(server_url + "/accounts/create/", data = {'username': new_username})        if not (r.status_code == 200):            print "Network failure."def delete_account(username):    if (logged_in):        r = requests.post(server_url + "/accounts/delete/", data = {'username': username})        if not (r.status_code == 200):            print "Network failure."        username = ""        logged_in = false    else:        print "Can't delete account when not logged in"def logout():    global logged_in    global username    if not logged_in:        print "Already logged out"    else:        r = requests.post(server_url + "/accounts/logout/", data = {'username': username})        if not (r.status_code == 200):             print "Network failure."        else:            username = ""            logged_in = Falsedef get_accounts(query):    r = requests.get(server_url + "/accounts/search/" + query)    if not (r.status_code == 200):        print "Network failure."    print r.json()    parsed_json = json.loads(r.json())    return parsed_json['accounts']def print_all_users():    all_accounts = get_accounts("*")    for account in all_accounts:        print accountdef help():    print "login <username>"    if not logged_in:        print "createaccount <username>"    if logged_in:        print "other stuff"def parse_command(command_arr):    if (command_arr[0] == "help"):        if (len(command_arr) != 1):            print incorrect_str        else:            help()    elif (command_arr[0] == "login"):        if (len(command_arr) != 2):            print incorrect_str        else:            login(command_arr[1])    elif (command_arr[0] == "createaccount"):        if (len(command_arr) != 2):            print incorrect_str        else:            create_account(command_arr[1])    elif (command_arr[0] == "allusers"):        if (len(command_arr) != 1):            print incorrect_str        else:            print_all_users()    elif (command_arr[0] == "logout"):        if (len(command_arr) != 1):            print incorrect_str        else:            logout()    else:         print "Entered invalid command."if __name__ == '__main__':    print "Welcome to dumb chat"    print "To see available commands, type help"    while True:        command_str = "Enter a command: "        incorrect_str = "Incorrect num args"        command = raw_input(command_str)        command_arr = command.split()        parse_command(command_arr)